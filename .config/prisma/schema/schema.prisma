generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("PRISMA_DATABASE_URL") // uses connection pooling
}


enum Statuses {
  PENDING
  IN_PROGRESS
  RESOLVED
  CLOSED
  REJECTED
  COMPLETED
}